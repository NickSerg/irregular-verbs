'use strict';

describe('myApp.engView module', function() {

  beforeEach(module('myApp.engView'));

  describe('engView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var engViewCtrl = $controller('EngViewCtrl');
      expect(engViewCtrl).toBeDefinedS();
    }));

  });
});