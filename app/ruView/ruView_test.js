'use strict';

describe('myApp.ruView module', function() {

  beforeEach(module('myApp.ruView'));

  describe('ruView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ruViewCtrl = $controller('RuViewCtrl');
      expect(ruViewCtrl).toBeDefined();
    }));

  });
});